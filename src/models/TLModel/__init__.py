"""
Main module for Transfer Learning Model (TLModel) on images. Use this module
by running the follwing command at the root of the project :

```
    python3 -m src.preprocessing.TLModel --<arg>
```

It will run the model for the chosen argument.

You can find and edit the model configuration at
`src/preprocessing/TLModel/model_config.yaml`.
"""
